<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ACCANER</title>
    <link rel="icon" type="icon" href="/imagenes/LOGO SIN FONDO.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <style>
        .titulo {
            font-size: 32px;
            font-weight: bold;
            background: linear-gradient(120deg, #00aaff, #aa00ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .subtitulo {
            font-size: 24px;
        }
        .descripcion {
            font-size: 18px;
        }
    </style>
</head>
<body>
    <header class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="#">
                <img src="/imagenes/LOGO SIN FONDO.png" alt="Logo de la página" width="51" height="50">
                A C A N N E R
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/perfil"><img src="/imagenes/Perfil.png" alt="Logo de la página" width="50" height="50"></a>
                    </li>
                </ul>
            </div>
        </div>
    </header>
    <div class="container mt-5">
        <div class="text-center">
            <h1>Editar Horario</h1>
            <form method="post" action="/horarios/edit/<%= horario.id %>" class="w-50 mx-auto">
                <label for="dia">Día:</label>
                <input type="text" id="dia" name="dia" value="<%= horario.dia %>" required class="form-control"><br>
                <label for="hora_inicio">Hora de Inicio:</label>
                <input type="time" id="hora_inicio" name="hora_inicio" value="<%= horario.hora_inicio %>" required class="form-control"><br>
                <label for="hora_fin">Hora de Fin:</label>
                <input type="time" id="hora_fin" name="hora_fin" value="<%= horario.hora_fin %>" required class="form-control"><br>
                <button type="submit" class="btn btn-primary">Guardar Cambios</button>
            </form>
            <a href="/horarios" class="mt-3">Volver a la Lista de Horarios</a>
        </div>
    </div>
</body>
</html>

